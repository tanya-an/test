/*
 * PostsList Messages
 *
 * This contains all the text for the PostsList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PostsList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PostsList component!',
  },
});
